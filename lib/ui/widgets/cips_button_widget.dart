part of 'widget.dart';

class CipsButtonWidget extends StatelessWidget {
  const CipsButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text('Text'));
  }
}
